@model BibliotecaMVC.Models.Editorial

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Editorial</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @* <div class="form-group">
                <label asp-for="IdEditorial" class="control-label"></label>
                <input asp-for="IdEditorial" class="form-control" />
                <span asp-validation-for="IdEditorial" class="text-danger"></span>
            </div> *@
            <div class="form-group">
                <label asp-for="NombreEd" class="control-label"></label>
                <input asp-for="NombreEd" class="form-control" />
                <span asp-validation-for="NombreEd" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
            @if(ViewBag.Error != "")
            {
            <h3>@ViewBag.Error</h3>
            }
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
